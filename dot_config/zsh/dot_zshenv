
if [[ $OSTYPE == darwin* ]]; then
    # Begin added by argcomplete
    fpath=( /opt/homebrew/Cellar/python-argcomplete/3.5.3/libexec/lib/python3.13/site-packages/argcomplete/bash_completion.d "${fpath[@]}" )
    # End added by argcomplete
elif [[ $OSTYPE == linux* ]]; then
    # Begin added by argcomplete
    fpath=( /home/linuxbrew/.linuxbrew/Cellar/python-argcomplete/3.5.3/libexec/lib/python3.13/site-packages/argcomplete/bash_completion.d "${fpath[@]}")
    # End added by argcomplete
fi
